export function toUpperCase(value: string) {
    return value.toUpperCase();
}

export function snippet(value: string) {
    return value.slice(0, 100) + '....';
};

export function otherSinpper(value: string) {
    return value.slice(0,20) + '...';
}